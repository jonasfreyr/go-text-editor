import "fmt"

/*
    This is a multiline comment
    Here's the proof
*/

// Main function that will be run
func main() int{
    var thing bool
    fmt.Println(len("I got color"))

    if true || thing {
        return -1
    }
    return 0
}